import React from "react";
import AppLayout from "../components/appLayout";
const Home = (props) => {
  return (
    <AppLayout>
      <div>Hello, Next!</div>
    </AppLayout>
  );
};

export default Home;
